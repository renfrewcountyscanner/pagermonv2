# RPi image builds disabled.
# To re-enable, change 'workflow_dispatch' to 'push' trigger.

name: Build RPi Image

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RPi image builds are currently disabled."
