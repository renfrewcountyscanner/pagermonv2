# Docker image builds disabled.
# To re-enable, change 'workflow_dispatch' to 'push' trigger.

name: Publish Docker image

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docker image publishing is currently disabled."
