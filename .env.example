# PagerMon Docker Environment Configuration
# Copy this file to .env and adjust values

# Server settings
SERVER_PORT=3000
TZ=America/Toronto

# Client settings (only used when running with --profile client)
RTL_DEVICE=0
RTL_FREQ=148.5875M
POCSAG_MODE=POCSAG512
